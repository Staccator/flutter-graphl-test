name: graphql_repro
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  json_serializable: ^4.1.3
  graphql_flutter: ^5.0.0
  build_runner: ^2.0.5
  artemis: ^7.0.0-beta.17

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

